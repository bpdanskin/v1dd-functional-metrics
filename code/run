#!/usr/bin/env bash
set -ex

# Master script for the capsule: "Reproducible Run" executes this file.
HERE="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
export PYTHONPATH="$HERE/src${PYTHONPATH:+:$PYTHONPATH}"
python -u "$HERE/run_pipeline.py" "$@"
